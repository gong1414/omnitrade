# OmniTrade Frontend — local environment
# Copy to .env.local before running `npm run dev`

# Backend API base URL (used by Next.js rewrites + direct SWR fetches)
NEXT_PUBLIC_API_URL=http://localhost:8000
