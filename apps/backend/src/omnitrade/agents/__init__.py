"""Agents layer — LangGraph nodes, trading agent, sub-agents.

Per consensus plan §7 R9, ``langgraph`` may ONLY be imported from
``agents/think_node.py``. The outer trading loop lives at
``application/trading_loop.py`` as pure asyncio.
"""
